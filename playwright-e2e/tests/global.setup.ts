import { test as setup } from '@playwright/test';

// example given by playwright below - needs to be build out
setup('create new database', async ({ }) => {
  console.log('e.g. creating new database...');
  // Initialize the database
});